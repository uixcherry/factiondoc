## 🔧 **Общее описание**
---

## ⚙️ **Конфигурация**

### **Структура конфигурации**
```xml
<Configuration>
  <Factions>
    <Faction ID="1" Name="Альянс" LeaderPermission="faction.alliance.leader">
      <Rank Order="1" Permission="faction.member" CanPromote="false" CanDemote="false" CanInvite="false" CanKick="false" MaxPromoteToRank="0" />
      <Rank Order="2" Permission="faction.fighter" CanPromote="false" CanDemote="false" CanInvite="false" CanKick="false" MaxPromoteToRank="0" />
      <Rank Order="3" Permission="faction.sergeant" CanPromote="true" CanDemote="true" CanInvite="true" CanKick="true" MaxPromoteToRank="2" />
    </Faction>
  </Factions>
</Configuration>
```

### **Параметры фракции**
- **`ID`** - Уникальный идентификатор фракции *(byte)*
- **`Name`** - Название фракции *(отображается в чате)*
- **`LeaderPermission`** - Permission для лидера фракции

### **Параметры ранга**
- **`Order`** - Порядковый номер ранга *(1 = низший, чем больше = выше)*
- **`Permission`** - Permission группа для этого ранга
- **`CanPromote`** - Может ли повышать других игроков
- **`CanDemote`** - Может ли понижать других игроков  
- **`CanInvite`** - Может ли приглашать новых игроков
- **`CanKick`** - Может ли исключать игроков
- **`MaxPromoteToRank`** - До какого ранга может повышать *(0 = без ограничений)*

---

## 🎮 **Команды**

### **Основная команда**
```
/faction <подкоманда> [параметры]
/f <подкоманда> [параметры]
```

**Permission:** `faction.use`

---

### **📨 Приглашения**

#### **`/faction invite <игрок>`**
**Алиасы:** `/f inv <игрок>`

**Описание:** Приглашает игрока во фракцию

**Требования:**
- ✅ Игрок должен состоять во фракции
- ✅ Игрок должен иметь право `CanInvite` или быть лидером
- ✅ Цель не должна состоять во фракции
- ✅ У цели не должно быть активного приглашения
- ✅ Нельзя приглашать самого себя
- ✅ Должен пройти кулдаун **30 секунд** между приглашениями

**Примеры:**
```
/faction invite Steve
/f inv PlayerName
```

**Ответы системы:**
- 🟢 **Успех:** `"Вы пригласили Steve во фракцию!"`
- 🔴 **Ошибки:** 
  - `"Вы не состоите ни в одной фракции!"`
  - `"У вас нет прав для приглашения игроков!"`
  - `"Этот игрок уже состоит во фракции!"`
  - `"Вы недавно отправляли приглашение. Подождите немного!"`

---

#### **`/faction accept`**
**Алиасы:** `/f acc`

**Описание:** Принимает приглашение во фракцию

**Особенности:**
- ⏰ Приглашение действует **3 минуты**
- 🎯 Игрок автоматически получает **низший ранг** фракции
- 🧹 Приглашение удаляется после принятия

**Примеры:**
```
/faction accept
/f acc
```

---

#### **`/faction decline`**
**Алиасы:** `/f dec`

**Описание:** Отклоняет приглашение во фракцию

**Примеры:**
```
/faction decline
/f dec
```

---

### **📈 Управление рангами**

#### **`/faction promote <игрок>`**
**Алиасы:** `/f prom <игрок>`

**Описание:** Повышает игрока в звании

**Требования:**
- ✅ Оба игрока должны быть в **одной фракции**
- ✅ Повышающий должен иметь право `CanPromote`
- ✅ У цели должен быть следующий ранг для повышения
- ✅ Повышающий не должен превышать свой лимит `MaxPromoteToRank`

**Логика повышения:**
```
Текущий ранг (Order: 2) → Следующий ранг (Order: 3)
```

**Примеры:**
```
/faction promote Steve
/f prom PlayerName
```

---

#### **`/faction demote <игрок>`**
**Алиасы:** `/f dem <игрок>`

**Описание:** Понижает игрока в звании

**Требования:**
- ✅ Оба игрока должны быть в **одной фракции**
- ✅ Понижающий должен иметь право `CanDemote`
- ✅ У цели должен быть предыдущий ранг для понижения

**Логика понижения:**
```
Текущий ранг (Order: 3) → Предыдущий ранг (Order: 2)
```

**Примеры:**
```
/faction demote Steve
/f dem PlayerName
```

---

### **👢 Исключение и выход**

#### **`/faction kick <игрок>`**
**Описание:** Исключает игрока из фракции

**Требования:**
- ✅ Оба игрока должны быть в **одной фракции**
- ✅ Исключающий должен иметь право `CanKick`

**Результат:**
- 🚫 Игрок теряет **все permissions** фракции
- 📢 Оба игрока получают уведомления

**Примеры:**
```
/faction kick Steve
```

---

#### **`/faction leave`**
**Описание:** Покидает текущую фракцию

**Ограничения:**
- ❌ **Лидер фракции НЕ МОЖЕТ покинуть фракцию**
- ✅ Обычные участники могут свободно выходить

**Результат:**
- 🚫 Игрок теряет **все permissions** фракции

**Примеры:**
```
/faction leave
```

---

### **📊 Информация**

#### **`/faction info`**
**Описание:** Показывает информацию о текущей фракции игрока

**Отображаемая информация:**
- 🏷️ Название фракции
- 🎖️ Текущее звание (permission группа)
- 👑 Статус лидера (Да/Нет)
- 🔧 Доступные права:
  - Приглашать
  - Повышать
  - Понижать  
  - Исключать

**Пример вывода:**
```
=== Информация о фракции ===
Фракция: Альянс
Звание: faction.sergeant
Лидер: Нет
Права: Приглашать(Да), Повышать(Да), Понижать(Да), Исключать(Да)
```

---

## 🛡️ **Системы безопасности**

### **🚫 Анти-спам защита**
- **Кулдаун приглашений:** 30 секунд между отправками
- **Время жизни приглашения:** 3 минуты
- **Одно активное приглашение** на игрока

### **🔒 Ограничения фракций**
- **Одна фракция на игрока** - невозможно состоять в нескольких
- **Изоляция фракций** - нельзя управлять игроками из других фракций
- **Защита лидера** - лидер не может покинуть фракцию

### **⚡ Автоочистка**
- **Истекшие приглашения** удаляются каждые 60 секунд
- **Данные в памяти** - не сохраняются между перезапусками

---

## 🔧 **Администрирование**

### **Настройка новой фракции**
1. Добавьте новую секцию `<Faction>` в конфигурацию
2. Задайте уникальный `ID` и `Name`
3. Создайте `LeaderPermission` группу в RocketMod
4. Настройте ранги по порядку от низшего к высшему
5. Перезагрузите плагин: `/rocket reload Forge.Faction`

### **Назначение лидера**
```bash
/p add <игрок> <LeaderPermission>
# Пример: /p add Steve faction.alliance.leader
```

### **Добавление игрока в фракцию**
```bash
/p add <игрок> <RankPermission>
# Пример: /p add PlayerName faction.member
```

### **Удаление из фракции**
```bash
/p remove <игрок> <AllFactionPermissions>
```

---

## 🐛 **Troubleshooting**

### **Игрок не может использовать команды**
1. ✅ Проверьте permission: `faction.use`
2. ✅ Убедитесь, что игрок подключен как Player (не Console)

### **Команды повышения/понижения не работают**
1. ✅ Проверьте настройку `CanPromote`/`CanDemote` в конфигурации
2. ✅ Убедитесь, что `Order` рангов правильно настроен
3. ✅ Проверьте `MaxPromoteToRank` ограничения

### **Игрок не получает приглашения**
1. ✅ Проверьте, что у отправителя есть право `CanInvite`
2. ✅ Убедитесь, что получатель не состоит в другой фракции
3. ✅ Проверьте кулдаун отправителя (30 секунд)

### **Приглашения исчезают**
1. ✅ Приглашения действуют только **3 минуты**
2. ✅ Приглашения **не сохраняются** при перезапуске сервера
3. ✅ Автоочистка происходит каждые 60 секунд
---

*Документация актуальна для версии плагина на C# 7.3 с RocketMod*
